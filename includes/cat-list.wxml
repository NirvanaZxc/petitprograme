<scroll-view scroll-x="true" class="header">
  <view class="nav">
    <view class="{{ catType === item.id ? 'selected' : ''}}"  wx:for="{{category}}" wx:key="id" data-type="{{item.id}}" bindtap="changePOIType">{{item.name}}</view>
  </view>
</scroll-view>
